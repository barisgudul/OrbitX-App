// app/(tabs)/index.tsx içine
import { Text, View } from 'react-native';

export default function DovizScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Altın Ekranı</Text>
    </View>
  );
}